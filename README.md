## Problem Statement
- Design and implement a backend system that converts long URLs into short, unique URLs and supports fast redirection back to the original URLs. The system should ensure uniqueness, handle expiration, and provide constant-time lookup for encode and decode operations.
